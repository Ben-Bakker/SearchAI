{
  "version": 2,
  "regions": ["fra1"],
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/search",
      "methods": ["POST", "OPTIONS"],
      "dest": "api/search/index.js"
    }
  ]
}
